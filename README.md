Program 3: Alicia Maranville & Fred Solis

Directory:
- `data` folder: Holds train and test data files.
- `models` folder: Holds the pkl files for each trained machine learning model (one for each word)
- `results` folder: Contains a text file for each word with each line storing the sense of the corresponding word in each sentence (1 or 2).
- `train_models.py`: Script to train the ML tools
- `cs5322f25prog3.py`: Script that generates results by disambiguating each word.
- `results.py`: Generates the result text files using functions from `cs5322f25prog3.py`
-  `NLP_Program_3.pdf`: PDF report detailing the method for classification